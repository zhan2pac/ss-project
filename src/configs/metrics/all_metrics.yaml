device: "auto"
audio_only: false
train: []
inference:
  - _target_: src.metrics.SiSNR
    device: ${metrics.device}
    name: "SI_SNR"
  - _target_: src.metrics.SiSNRi
    device: ${metrics.device}
    name: "SI_SNRi"
  - _target_: src.metrics.SDRi
    device: ${metrics.device}
    name: "SDRi"
  - _target_: src.metrics.PESQ
    device: ${metrics.device}
    fs: ${datasets.train.target_sr}
    mode: "wb"
    name: "PESQ"
  - _target_: src.metrics.STOI
    device: ${metrics.device}
    fs: ${datasets.train.target_sr}
    name: "STOI"
